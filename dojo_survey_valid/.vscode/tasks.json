{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/dojo_survey_valid.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}